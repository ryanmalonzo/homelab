apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-regular-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  selector:
    matchLabels:
      name: sonarr-regular-config
