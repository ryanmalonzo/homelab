apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3600Gi
  storageClassName: ""
  selector:
    matchLabels:
      name: media
