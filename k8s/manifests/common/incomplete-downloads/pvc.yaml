apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: incomplete-downloads
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: ""
  selector:
    matchLabels:
      name: incomplete-downloads
