apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: profilarr-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  selector:
    matchLabels:
      name: profilarr-config
