apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-anime-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  selector:
    matchLabels:
      name: radarr-anime-config
