apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-anime-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  selector:
    matchLabels:
      name: sonarr-anime-config
